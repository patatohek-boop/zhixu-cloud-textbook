---json
{
  "id": "python-01",
  "title": "第一段程序：环境、交互式与脚本",
  "group": "01 · 计算思维与语言基础",
  "minutes": 20,
  "level": "基础",
  "tags": [
    "环境",
    "脚本",
    "REPL"
  ],
  "objectives": [
    "识别 REPL 与终端",
    "运行并修改一个脚本"
  ],
  "prerequisites": [
    "基本电脑操作"
  ],
  "summary": "区分解释器、编辑器、终端和程序文件，建立先预测再运行的习惯。",
  "quiz": {
    "question": "以下哪项是可复现脚本应具备的性质？",
    "options": [
      "依赖上次交互式环境中的变量",
      "从干净进程顺序运行即可得到结果",
      "必须联网",
      "只能有一行代码"
    ],
    "answer": 1,
    "explanation": "脚本应显式提供输入和依赖，不能依靠交互式环境残留状态。"
  },
  "lab": null
}
---

## 为什么从一行输出开始
程序是一套可以重复执行的精确步骤。计算机不会补齐“差不多”“适量”之类的隐含意思，所以编程首先是把问题说清楚。可以把解释器想成翻译员：它按 Python 的语法理解指令；编辑器负责写字，终端负责启动程序，它们不是同一个东西。网站展示代码不等于在浏览器中启动完整 Python，以下代码请复制到本机运行。

## 建立最小可用环境
安装 Python 3 的受支持版本后，在系统终端输入 `python --version` 检查解释器；Windows 也可能使用 `py --version`。不同电脑启动命令可能不同，应以实际输出为准。输入 `python` 进入交互式环境后会看到 `>>>`，它是提示符，不是要复制的代码。交互式环境适合试算；脚本适合保存完整工作。新建 UTF-8 编码的 `hello.py`，写入下面三行，然后在文件所在目录执行 `python hello.py`。

```python
name = "学习者"
minutes = 25
print(f"{name}，今天专注学习 {minutes} 分钟。")
```

第一行把名字绑定到一段文字，第二行保存一个整数，第三行将二者组合后显示。输出应为“学习者，今天专注学习 25 分钟。”。把 25 改为 40，重新保存并运行，观察输出变化。这种“预测—实验—解释”的循环，比一次性复制很多代码更能建立理解。

## 输入、处理、输出
把任何小程序画成三格：输入是什么，按照什么规则处理，输出是什么。本例输入是写死在文件里的名字和时长，处理是格式化，输出是终端文字。下一步可以将第一行改成 `name = input("你的名字：")`。注意 `input` 返回字符串，即使输入 25，也暂时是文字而不是数值。读取用户输入会等待键盘操作，自动运行时应避免无提示地等待。

脚本从上往下执行，所以把输出放在变量定义之前会报错。交互式环境会保留之前的变量，容易让缺少定义的片段“碰巧能运行”；验证脚本时应从干净进程开始。Notebook 也有类似问题：单元格显示顺序不一定是实际执行顺序。可复现的程序应当从第一步顺序执行即可得到结果。

## 常见问题与排查
若提示找不到命令，检查安装和解释器路径；若提示找不到文件，检查终端当前目录和文件名；若出现语法错误，检查英文引号、括号和缩进。不要把错误都归因于“电脑坏了”。完整错误信息中的文件、行号、异常名称是定位线索。先保存文件再运行，并确认文件没有被编辑器保存成 `hello.py.txt`。

## 小实验：程序执行与保存是两回事
将名字改为自己的昵称后，先故意不保存就运行一次，再保存后运行一次。前一次可能仍显示旧结果，因为解释器读取的是磁盘文件，不是编辑器尚未保存的内容。再把输出语句复制到文件末尾，确认它会执行两次。通过这些小变化，可以把“屏幕显示”“内存状态”“文件内容”三者分开理解。

## 练习
1. 不运行代码，预测把 `minutes = 25` 改为 `minutes = 25 + 5` 后的结果，再验证。
<details><summary>查看解析</summary>表达式先计算成整数 30，再绑定到 minutes；最终输出的时长为 30。赋值保存的是求值结果，不是原来的算式文字。</details>

2. 为什么一段代码在交互式环境中能运行，放进新脚本却出现名字未定义？
<details><summary>查看解析</summary>交互式环境可能保留了此前定义的变量。新脚本是一个新的执行过程，需要显式提供所有输入和定义。这也是重启后重新运行的重要原因。</details>
