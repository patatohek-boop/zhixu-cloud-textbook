---json
{
  "id": "python-03",
  "title": "数值计算、浮点误差与单位",
  "group": "01 · 计算思维与语言基础",
  "minutes": 20,
  "level": "基础",
  "tags": [
    "数值",
    "浮点数",
    "单位"
  ],
  "objectives": [
    "选择正确数值类型",
    "使用容差比较浮点数"
  ],
  "prerequisites": [
    "python-02"
  ],
  "summary": "掌握算术运算，并把近似误差、有效数字和物理单位纳入计算。",
  "quiz": {
    "question": "把厘米表示的半径换成米，圆面积数值会缩小多少倍？",
    "options": [
      "10",
      "100",
      "1000",
      "10000"
    ],
    "answer": 3,
    "explanation": "长度缩小 100 倍，面积随长度平方缩小 10000 倍。"
  },
  "lab": null
}
---

## 数学表达式如何进入程序
Python 用 `+`、`-`、`*`、`/` 表示四则运算，`**` 表示乘方。`/` 做真除法，`//` 向负无穷取整，`%` 给出相应余数。例如 `-7 // 3` 是 -3，且恒等式 `a == (a//b)*b + a%b` 对非零整数 b 成立。括号既能改变优先级，也能向读者说明分组；对于关键公式，宁可写清楚，不要考验读者的记忆。

## 浮点数为何不是精确小数
十进制的三分之一不能写成有限小数，类似地，十进制的 0.1 在二进制中也通常需要无限位。计算机只能保存有限位，所以 `0.1 + 0.2` 的结果可能显示为 `0.30000000000000004`。这不是加法失效，而是输入和运算都发生了舍入。使用 `round` 改变显示或得到舍入值，不会让后续所有计算变成精确实数运算。

```python
import math
from decimal import Decimal
print(0.1 + 0.2 == 0.3)              # False
print(math.isclose(0.1 + 0.2, 0.3))  # True
print(Decimal("0.1") + Decimal("0.2"))
radius_m = 0.05
area_m2 = math.pi * radius_m ** 2
print(f"面积约为 {area_m2:.6f} 平方米")
```

比较近似值应采用绝对容差与相对容差。`math.isclose(a,b)` 的核心规则是差值不超过相对尺度容差与绝对容差中的较大者。接近零时必须根据问题尺度设置 `abs_tol`；否则仅靠相对误差可能无法判断“小到可以忽略”。金额需要十进制规则时可使用从字符串构造的 `Decimal`，分数可用 `Fraction`；它们也有成本和适用边界。

## 逐步例题：圆管截面
半径为 5 厘米的圆管，先换成 0.05 米，再计算 $A=\pi r^2$，得到约 $0.007854\ \mathrm{m^2}$。若直接把 5 代入并把结果标成平方米，会错一万倍，因为面积的单位转换要平方。变量名写入单位可以显著减少这类错误。程序默认不会检查米和厘米能否相加，量纲责任仍属于建模者。

进行连续相减或求和时也要留意误差放大。两个很接近的大数相减可能损失有效数字；大量小数求和可考虑 `math.fsum`。输出很多位不意味着测量有那么高的精度。输入半径只有两位有效数字，就不应把计算结果的十几位小数当成实验事实。

## 用误差预算决定容差
假设长度测量只精确到十分之一毫米，面积计算显示十几位小数也不会增加物理信息。先估计输入的不确定性，再确定输出保留位数和验证容差。对于圆面积，小的半径相对误差会近似放大为两倍的面积相对误差；这来自对面积公式求导，而不是浮点计算本身。
比较两个算法时，可同时报告绝对差与相对差。例如参考值约为一百万，差一可能很小；参考值约为百万分之一，差一却非常大。接近零时，相对差会不稳定，所以需要独立的绝对尺度。
还要留意异常数值的传播：无穷大与非数值可能让后续平均、比较或绘图失真。输入阶段使用有限性检查，结果阶段检查量纲和合理范围。程序没有抛异常，只说明运算被执行，并不保证数值结论符合问题。

容差是科学判断的一部分，必须保留依据。

## 练习
1. `7/2`、`7//2`、`7%2` 分别是什么？
<details><summary>查看解析</summary>分别为 3.5、3、1。整数商与余数满足 7=3×2+1；真除法保留小数部分。</details>

2. 检查结果是否接近零，应仅使用默认相对容差吗？
<details><summary>查看解析</summary>通常不够。应根据量纲和问题精度设置绝对容差，例如 abs_tol=1e-9；不存在对所有物理量都合适的统一容差。</details>
