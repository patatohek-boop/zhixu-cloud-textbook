---json
{
  "id": "python-06",
  "title": "循环、不变量与终止",
  "group": "01 · 计算思维与语言基础",
  "minutes": 20,
  "level": "基础",
  "tags": [
    "循环",
    "不变量",
    "终止"
  ],
  "objectives": [
    "跟踪累计变量",
    "说明循环终止条件"
  ],
  "prerequisites": [
    "python-05"
  ],
  "summary": "掌握 for 和 while，并通过不变量解释循环为何正确。",
  "quiz": {
    "question": "循环不变量描述的是什么？",
    "options": [
      "永不改变的输入类型",
      "每轮执行中保持成立的关系",
      "循环必须执行的次数",
      "所有变量都不能修改"
    ],
    "answer": 1,
    "explanation": "不变量是每轮保持成立的性质，不要求具体数值不变化。"
  },
  "lab": "python-loop"
}
---

## 重复不是复制粘贴
当同一规则要处理一组数据时，用循环明确表达“对每一项做什么”。`for` 遍历可迭代对象，适合已知数据集合；`while` 在条件成立时重复，适合迭代次数由计算过程决定的任务。`range(n)` 依次给出 0 到 n-1，右端不包含使长度恰好为 n。

```python
values = [18.0, 20.0, 22.0]
total = 0.0
count = 0
for value in values:
    total += value
    count += 1
if count == 0:
    print("没有可计算的数据")
else:
    print(total / count)  # 20.0
```

在第一次循环前，total 是零。处理前 k 个数据之后，total 恰好等于这 k 个数之和，count 恰好等于 k；这条每轮都保持成立的陈述叫循环不变量。最后 k 等于数据总数，所以 total/count 确实是算术平均。理解不变量，比单纯记住模板更容易发现“累加器放错位置”的问题。

## 逐步追踪状态

<figure class="teaching-figure"><a href="assets/diagrams/python-loop-accumulation.svg" target="_blank" rel="noopener" aria-label="打开大图：每轮把当前值加入 total；处理前 k 项后，total 等于这 k 项之和，这就是循环不变量。"><img src="assets/diagrams/python-loop-accumulation.svg" alt="每轮把当前值加入 total；处理前 k 项后，total 等于这 k 项之和，这就是循环不变量。" loading="lazy"></a><figcaption>每轮把当前值加入 total；处理前 k 项后，total 等于这 k 项之和，这就是循环不变量。<br><small>输入为 [18, 20, 22]，total 在循环外初始化为零；本图仅展示求和，平均值还需除以有效计数。 · 点按图形可放大。</small></figcaption></figure>


对本例画三列表格：当前 value、更新后 total、更新后 count。三轮结果分别是 (18,18,1)、(20,38,2)、(22,60,3)。如果把 total=0 放进循环，每轮都会清空历史，最终只剩最后一个值。程序错误常来自状态在何处建立、何时更新，而不是某个加法算错。

while 循环还要说明终止理由。例如从 n=5 开始，每轮 n 减一，条件为 n>0，那么非负整数 n 严格下降，最终会到零。如果每轮错误地增加 n，条件将一直成立。真实数值迭代应同时设置容差与最大迭代次数，避免因浮点误差或不收敛无限运行。

## break、continue 与循环 else
`break` 立即离开最近一层循环；`continue` 跳过本轮剩余语句，进入下一轮。循环后的 else 在循环正常结束而未被 break 中断时执行，可用于表达“遍历完仍未找到”。它与 if 的 else 不同，初学时可以先用显式变量表达结果，再学习这种紧凑写法。

遍历列表时通常不要同时删除其中的元素，因为索引移动会导致跳过数据。可创建新列表，或遍历副本。嵌套循环的工作量相乘：外层 n 次、内层 m 次，主体通常执行 nm 次。输入增大一百倍，运行时间不一定只增大一百倍，算法结构决定扩展成本。

## 从循环表检查边界
计数任务最常见的错误是多一次或少一次。写下循环开始前、每轮开始、每轮结束和全部结束后的状态，确认索引始终在允许范围内。若处理长度为零或一的序列，很多隐藏假设会立刻暴露，所以它们应成为最早测试的例子。
累加和累乘的初始值不同：求和从零开始，因为加零不改变结果；求积从一开始，因为乘一不改变结果。寻找最小值时不要随意用零初始化，否则全为正数的输入会错误得到零；可从首元素开始并明确处理空输入。
对循环搜索，找到目标后是否停止取决于任务。找第一个匹配可中断，找全部匹配则应继续。把“找到没有”和“找到多少”混用，常导致看似正确的示例在重复数据上失败。始终先写清楚输出契约，再选择控制语句。

## 练习
1. `sum(range(1, 5))` 为何是 10 而不是 15？
<details><summary>查看解析</summary>range 的终点不包含，因此只包含 1、2、3、4，总和为 10。要包含 5，可使用 range(1, 6)。</details>

2. 若累计平均值时将 count 的初始化放入循环，会出现什么？
<details><summary>查看解析</summary>count 无法记录历史数量，最后通常只剩 1，导致总和被错误地除以 1。累加状态应在循环前建立。</details>
