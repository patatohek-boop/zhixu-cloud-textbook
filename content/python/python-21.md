---json
{
  "id": "python-21",
  "title": "pandas 表格、缺失与连接",
  "group": "05 · 科学计算与数据",
  "minutes": 25,
  "level": "进阶",
  "tags": [
    "pandas",
    "缺失值",
    "连接"
  ],
  "objectives": [
    "验证数据粒度",
    "检查连接是否扩增记录"
  ],
  "prerequisites": [
    "python-20"
  ],
  "summary": "明确一行的含义，再进行筛选、聚合、缺失处理和表格连接。",
  "quiz": {
    "question": "many_to_one 连接校验要求哪一侧键唯一？",
    "options": [
      "左侧",
      "右侧",
      "两侧均可重复",
      "两侧必须完全相同"
    ],
    "answer": 1,
    "explanation": "多个左侧观测可以对应一个右侧元数据记录，右侧键须唯一。"
  },
  "lab": null
}
---

## 表格不是没有类型的网格
DataFrame 是带行索引与列标签的表格结构，各列可以拥有不同类型。开始分析前先回答：一行代表什么，哪几列组合应唯一，缺失意味着什么，数值单位是什么。若一行是一次观测，把它当成一台设备，就可能重复计数。工具越方便，越要先确认数据语义。

```python
import pandas as pd

observations = pd.DataFrame({
    "sensor": ["A", "A", "B"],
    "temp_c": [20.0, None, 24.0]
})
metadata = pd.DataFrame({"sensor": ["A", "B"], "room": ["北", "南"]})
joined = observations.merge(metadata, on="sensor", how="left", validate="many_to_one")
print(joined)
summary = joined.groupby("sensor")["temp_c"].agg(["count", "mean"])
print(summary)
print(observations["temp_c"].isna().sum())  # 1
```

mean 默认通常跳过缺失值，因此 A 的平均为 20，并不表示缺失值自动测得 20。count 统计非缺失值，size 统计行数，二者有不同含义。报告平均值时同时报告有效样本量，可以避免把一个观测与一百个观测看成同等可信。

## 选择与转换
loc 按标签和条件选择，iloc 按整数位置选择。筛选温度大于 21 可写 `df.loc[df["temp_c"] > 21]`。赋值时使用明确的单次 loc 选择，避免链式索引导致操作副本还是原表不清楚。读取 CSV 后应检查 dtypes，日期用明确格式解析，数值转换失败时要保留错误证据。

缺失处理没有万能答案。删除可能改变样本组成，填均值会压低方差，前向填充需要时间顺序和业务依据。对于机器学习，填补统计量只能从训练数据学习；先在全数据上填均值再切分会把测试信息带入训练过程。

## 连接的乘法效应
如果左表某键出现两次，右表同键出现三次，多对多连接会产生六行。这不是软件故障，而是所有匹配组合的结果。应检查键唯一性，并使用 validate 声明预期关系。left 连接保留左表行，inner 只保留两侧匹配键；连接方式会改变分析总体，不能仅凭“结果更整齐”选择。

聚合后粒度发生变化，例如从观测变成设备。图表、统计与模型都应跟随新粒度解释。不要把设备均值再不加权平均后当作所有观测的总体均值，除非每台设备样本数相同或目标本就要求设备等权。

## 聚合结果要保留分母
假设设备 A 有一个读数十度，设备 B 有九个读数二十度。先求设备均值再等权平均得到十五度；对全部十次观测平均得到十九度。两者都可计算，但回答的问题不同：前者给设备等权，后者给观测等权。
因此报告应同时保留有效数量、总数量和缺失数量，说明目标总体。若采样频率不同，观测加权还可能让高频设备主导结论，需要根据任务选择时间或设备权重。
表格索引可以提高对齐便利性，也可能让看似按位置相加的操作实际按标签对齐。合并、赋值和算术后检查行数、键、缺失和汇总量，能发现很多静默错误。不要只查看前五行就认为整张表已经正确。

任何行数变化都应能够解释清楚。

## 练习
1. 某组有 5 行，其中 2 行缺失，count 与 size 分别是多少？
<details><summary>查看解析</summary>对该数值列 count 为 3，size 为 5。前者是有效观测数，后者是记录总数。</details>

2. 连接后行数翻倍，应先检查什么？
<details><summary>查看解析</summary>检查连接键在两表中的重复次数与预期关系，再检查连接方式。不要直接去重，因为新增行可能暴露真实粒度问题。</details>
